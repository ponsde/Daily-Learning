这题在touch2有个检查rdi的情况
因此需要将cookie值压入rdi，然后才能通过touch2

通过汇编代码movq和ret，然后gcc和objdump得到机器码

注意cookie值前面要加$表示这是立即数，不然会被认为是内存寻址，是个内存

然后去找%rsp的地址

%rsp扩展后的地址为 0x55681738 把得到的机器码放在这位置，等ret的时候就ret到这里执行mov指令

注意ret是在rsp的指针处pop出8字节，因此第二个返回地址应该放在后面而不是前面

